"use client";
import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export type TrendsData = { rescued: { date: string; pounds: number; rescues: number }[]; scores: { date: string; averageMatchScore: number }[] };
export type CategoryData = { categories: { category: string; suppliedLbs: number; neededLbs: number }[] };
export function InsightsCharts({ trends, categories }: { trends: TrendsData; categories: CategoryData }) { return <div className="insights-grid"><article className="chart-card"><h3>Food rescued over time</h3>{trends.rescued.length ? <ResponsiveContainer width="100%" height={300}><LineChart data={trends.rescued}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="date" /><YAxis /><Tooltip /><Line type="monotone" dataKey="pounds" stroke="#2f8f46" strokeWidth={3} /></LineChart></ResponsiveContainer> : <p className="empty-state">No completed rescues in this period.</p>}</article><article className="chart-card"><h3>Supply and current demand</h3>{categories.categories.length ? <ResponsiveContainer width="100%" height={300}><BarChart data={categories.categories}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="category" /><YAxis /><Tooltip /><Legend /><Bar dataKey="suppliedLbs" name="Supplied lbs" fill="#65a832" /><Bar dataKey="neededLbs" name="Needed lbs" fill="#ee8b25" /></BarChart></ResponsiveContainer> : <p className="empty-state">No category data in this period.</p>}</article><article className="chart-card chart-card--wide"><h3>Average match score over time</h3>{trends.scores.length ? <ResponsiveContainer width="100%" height={280}><LineChart data={trends.scores}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="date" /><YAxis domain={[0,100]} /><Tooltip /><Line type="monotone" dataKey="averageMatchScore" stroke="#0b4f3c" strokeWidth={3} /></LineChart></ResponsiveContainer> : <p className="empty-state">No match scores in this period.</p>}</article></div>; }
