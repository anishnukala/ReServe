"use client";
import { divIcon } from "leaflet";
import { MapContainer, Marker, Popup, TileLayer } from "react-leaflet";
export interface NearbyOrganization { id: string; name: string; address: string; latitude: number; longitude: number; distanceMiles: number; acceptedCategories?: string[]; availableCapacityLbs?: number; verified?: boolean; profileComplete: boolean; currentNeeds: { foodCategory: string; urgencyScore: number }[]; }
const markerIcon = (kind: "you" | "organization") => divIcon({ className: `map-pin map-pin--${kind}`, html: `<span>${kind === "you" ? "You" : "R"}</span>`, iconSize: [38, 38], iconAnchor: [19, 38] });
export function OrganizationMap({ latitude, longitude, organizations }: { latitude: number; longitude: number; organizations: NearbyOrganization[] }) { return <MapContainer className="map-panel" center={[latitude, longitude]} zoom={11} scrollWheelZoom><TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><Marker position={[latitude, longitude]} icon={markerIcon("you")}><Popup>Your search location</Popup></Marker>{organizations.map((organization) => <Marker key={organization.id} position={[organization.latitude, organization.longitude]} icon={markerIcon("organization")}><Popup><strong>{organization.name}</strong><br />{organization.distanceMiles} miles away<br />{organization.currentNeeds.length ? `Needs: ${organization.currentNeeds.map((need) => need.foodCategory).join(", ")}` : "No current needs posted"}</Popup></Marker>)}</MapContainer>; }
