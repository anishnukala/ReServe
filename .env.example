# Set true to run the project without MongoDB or external API keys.
DEMO_MODE=true

MONGODB_URI=
MONGODB_DB=reserve

# Server-side key for Google Places API (New)
GOOGLE_MAPS_API_KEY=

# Optional: used for food-description extraction
OPENAI_API_KEY=
